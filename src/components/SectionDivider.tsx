import React from 'react';

const SectionDivider = () => {
  return (
    <div className="relative w-full py-8 sm:py-10 md:py-12 overflow-hidden bg-gradient-to-b from-green-800/20 via-green-700/30 to-green-700">
      {/* Clean gradient transition without decorative elements */}
    </div>
  );
};

export default SectionDivider;
